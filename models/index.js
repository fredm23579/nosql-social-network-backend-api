const User = require('./User');
const Thought = require('./Thought');
const Reaction = require('./Reaction'); // Import the Reaction schema

module.exports = { User, Thought, Reaction }; // Export it alongside User and Thought
